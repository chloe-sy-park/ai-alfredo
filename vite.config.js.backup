import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import { VitePWA } from 'vite-plugin-pwa'

// PWA 설정은 나중에 구현 예정
// 필요한 패키지: vite-plugin-pwa, workbox-precaching, workbox-routing, workbox-strategies, workbox-expiration

export default defineConfig({
  plugins: [
    react(),
    /* PWA 설정 - 나중에 활성화
    VitePWA({
      // injectManifest 모드 - 커스텀 SW 사용
      strategies: 'injectManifest',
      srcDir: 'src',
      filename: 'sw.js',
      registerType: 'autoUpdate',
      injectRegister: 'auto',
      
      includeAssets: ['favicon.ico', 'icons/*.png', 'alfredo-*.svg'],
      
      manifest: {
        name: 'Life Butler - AI 알프레도',
        short_name: 'Life Butler',
        description: '당신의 AI 집사 알프레도가 일상을 관리해드려요',
        theme_color: '#A996FF',
        background_color: '#F0EBFF',
        display: 'standalone',
        orientation: 'portrait-primary',
        scope: '/',
        start_url: '/',
        categories: ['productivity', 'lifestyle'],
        icons: [
          {
            src: '/icons/icon-192x192.png',
            sizes: '192x192',
            type: 'image/png'
          },
          {
            src: '/icons/icon-512x512.png',
            sizes: '512x512',
            type: 'image/png'
          },
          {
            src: '/icons/icon-512x512.png',
            sizes: '512x512',
            type: 'image/png',
            purpose: 'maskable'
          }
        ]
      },
      
      injectManifest: {
        globPatterns: ['**/*.{js,css,html,ico,png,svg,woff,woff2}'],
        maximumFileSizeToCacheInBytes: 5 * 1024 * 1024 // 5MB
      },
      
      devOptions: {
        enabled: true,
        type: 'module'
      }
    })
    */
  ],
  build: {
    outDir: 'dist',
    sourcemap: false,
    minify: 'terser',
    terserOptions: {
      compress: {
        drop_console: true,
        drop_debugger: true
      }
    }
  }
})